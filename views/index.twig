
{% extends 'layout.twig' %}
{% block content %}
<section class="post-type-{{post.post_type}}" id="post-{{post.ID}}">
  {% for post in posts %}
  {% include ['custom-preview-'~post.post_type~'.twig', 'custom-preview.twig'] %}
  {% endfor %}
</section>{% endblock %}